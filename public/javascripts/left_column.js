//clear form with Clear All button
/*function clearForm() {
	$("#mainform")[0].reset();
}

// knockout
function MyModel() {
	var self = this;

	//
	$.getJSON("@{Application.getTeamList()}", function(data) {
		console.log(data);
		self.teamlist = data;
	});

	self.chosenTeam = ko.observable(teamlis[0]);

	self.chooseTeam = function(team) {
		console.log(team);
	}
	
};

ko.applyBindings(new MyModel());
*/